---
fixes:
  - |
    Fix empty ``else`` blocks generated by the ``service-start-order`` role
    that caused ``sh: "fi" unexpected`` syntax errors. The role now applies
    the configured post-healthy delay consistently when dependencies are
    already healthy or have no healthcheck.
